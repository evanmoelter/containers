---
app: par2cmdline-turbo
base: false
channels:
  - name: stable
    platforms: ["linux/amd64", "linux/arm64"]
    stable: true
    tests:
      # goss tests do not currently work for shell-less containers
      # see https://github.com/goss-org/goss/issues/870
      enabled: false
